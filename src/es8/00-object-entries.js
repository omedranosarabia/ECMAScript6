// Objet.entries returns an array of arrays with the key and value of the object
const counties = { MX: 'Mexico', CO: 'Colombia', AR: 'Argentina', PE: 'Peru', VE: 'Venezuela' };

const countriesEntries = Object.entries(counties);

console.log(countriesEntries);